collections:
  - name: servicenow.itsm
  - name: community.hashi_vault
  - name: ansible.eda
  # Automation Hub only (not on public Galaxy) — the build resolves it via
  # the ansible-config secret mounted at /etc/ansible by the BuildConfig.
  - name: redhat.insights
