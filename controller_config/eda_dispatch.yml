---
- name: Apply EDA Controller Configuration
  hosts: localhost
  connection: local
  gather_facts: false
  vars:
    cluster_domain: "{{ lookup('env', 'CONTROLLER_HOST') | default('', true) | regex_replace('^https?://aap-aap\\.', '') }}"
    eda_host: "{{ lookup('env', 'EDA_HOST') | default('https://aap-aap.' ~ cluster_domain, true) }}"
    eda_username: "{{ lookup('env', 'EDA_USERNAME') | default('admin', true) }}"
    eda_password: "{{ lookup('env', 'CONTROLLER_PASSWORD') }}"
    eda_validate_certs: false
    eda_config_base: "{{ playbook_dir }}/env/Default/env"
  pre_tasks:
    - name: Validate EDA connection
      ansible.builtin.assert:
        that:
          - eda_host | length > 0
          - eda_username | length > 0
          - eda_password | length > 0
        fail_msg: >-
          EDA connection not configured. Either attach an
          "AAP Controller Admin" credential or pass CONTROLLER_PASSWORD
          as an extra var.

    - name: Load EDA configuration from filetree
      ansible.builtin.include_vars:
        dir: "{{ item }}"
        extensions: ["yml", "yaml"]
      loop:
        - "{{ eda_config_base }}/common/eda_decision_environments.d"
        - "{{ eda_config_base }}/common/eda_projects.d"
        - "{{ eda_config_base }}/common/eda_rulebook_activations.d"
      loop_control:
        label: "{{ item | basename }}"

    - name: Ensure EDA controller token exists
      ansible.builtin.uri:
        url: "{{ eda_host }}/api/eda/v1/users/me/awx-tokens/"
        method: GET
        user: "{{ eda_username }}"
        password: "{{ eda_password }}"
        force_basic_auth: true
        validate_certs: "{{ eda_validate_certs }}"
        return_content: true
      register: _eda_tokens

    - name: Create EDA controller token if missing
      ansible.builtin.uri:
        url: "{{ eda_host }}/api/eda/v1/users/me/awx-tokens/"
        method: POST
        user: "{{ eda_username }}"
        password: "{{ eda_password }}"
        force_basic_auth: true
        validate_certs: "{{ eda_validate_certs }}"
        body_format: json
        body:
          name: EDA to Controller
          description: Token for EDA rulebook activations to trigger controller workflows
        status_code: [200, 201]
      when: (_eda_tokens.json.count | default(0)) == 0
  roles:
    - infra.eda_configuration.dispatch
