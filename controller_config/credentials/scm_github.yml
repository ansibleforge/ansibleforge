---
controller_credentials:
  - name: GitHub - AnsibleForge
    credential_type: Source Control
    organization: Default
    inputs:
      username: git
    input_sources:
      - source_credential: Vault Lookup
        target: password
        metadata:
          secret_path: "secret/data/github"
          secret_key: "token"
          secret_version: ""
