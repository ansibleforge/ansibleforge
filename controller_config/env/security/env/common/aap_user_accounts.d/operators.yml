---
# Demo placeholder users — real accounts go via Vault-backed passwords or SSO.
aap_user_accounts:
  - username: security-operator-1
    first_name: Security
    last_name: Operator1
    email: security-operator-1@example.com
    password: "{{ lookup('env', 'SECURITY_OPERATOR_DEFAULT_PASSWORD') | default('CHANGE_ME', true) }}"
    is_superuser: false
    state: present
