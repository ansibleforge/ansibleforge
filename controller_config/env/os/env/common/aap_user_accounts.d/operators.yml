---
# Demo placeholder users — real accounts go via Vault-backed passwords or SSO.
aap_user_accounts:
  - username: os-operator-1
    first_name: OS
    last_name: Operator1
    email: os-operator-1@example.com
    password: "{{ lookup('env', 'OS_OPERATOR_DEFAULT_PASSWORD') | default('CHANGE_ME', true) }}"
    is_superuser: false
    state: present
