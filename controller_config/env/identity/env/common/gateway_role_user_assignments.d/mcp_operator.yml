---
# Grants the MCP service user the Org Operator role on the Identity org.
# object_id resolved from org_id_by_name fact built in dispatch.yml.
gateway_role_user_assignments:
  - user: identity-mcp
    role_definition: Org Operator
    object_id: "{{ org_id_by_name['Identity'] }}"
    state: present
