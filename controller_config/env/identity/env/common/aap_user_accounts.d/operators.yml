---
# Demo placeholder users — real accounts go via Vault-backed passwords or SSO.
aap_user_accounts:
  - username: identity-operator-1
    first_name: Identity
    last_name: Operator1
    email: identity-operator-1@example.com
    password: "{{ lookup('env', 'IDENTITY_OPERATOR_DEFAULT_PASSWORD') | default('CHANGE_ME', true) }}"
    is_superuser: false
    state: present
