---
# Localhost-only inventory used by the Cancel Storage Job and Sync Storage CaC JTs.
# Both playbooks run on hosts: localhost — this gives the JT a valid inventory
# without dragging real targets into the org.
controller_inventories:
  - name: Demo Inventory
    description: Localhost inventory for cancel and sync job templates.
    organization: Storage
    state: present
