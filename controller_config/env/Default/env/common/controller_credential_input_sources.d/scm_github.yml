---
# Password minted per use by the GitHub App lookup (1h installation
# tokens) — replaced the expiring Vault-stored PAT.
controller_credential_input_sources:
  - source_credential: GitHub App - AnsibleForge Git
    target_credential: "GitHub - AnsibleForge"
    input_field_name: password
    metadata: {}
