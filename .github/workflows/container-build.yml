name: "Container: Build and Push"

on:
  push:
    branches: [main]
    paths:
      - "containers/tools-ansibleforge/**"
      - "containers/ee-ansibleforge/**"
      - "keycloak-extensions/github-org-mapper/**"
      - "containers/devfile-registry/**"
      - "devfile.yaml"
      - ".github/workflows/container-build.yml"
  workflow_dispatch:
    inputs:
      image:
        description: "Image to build"
        required: true
        type: choice
        options:
          - tools-ansibleforge
          - ee-ansibleforge
          - keycloak-github-org-mapper
          - devfile-registry
          - all

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}

jobs:
  detect-changes:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    outputs:
      tools-ansibleforge: ${{ steps.changes.outputs.tools-ansibleforge }}
      ee-ansibleforge: ${{ steps.changes.outputs.ee-ansibleforge }}
      keycloak-github-org-mapper: ${{ steps.changes.outputs.keycloak-github-org-mapper }}
      devfile-registry: ${{ steps.changes.outputs.devfile-registry }}
    steps:
      - uses: actions/checkout@v6
      - uses: dorny/paths-filter@v3
        id: changes
        with:
          filters: |
            tools-ansibleforge:
              - 'containers/tools-ansibleforge/**'
            ee-ansibleforge:
              - 'containers/ee-ansibleforge/**'
            keycloak-github-org-mapper:
              - 'keycloak-extensions/github-org-mapper/**'
            devfile-registry:
              - 'containers/devfile-registry/**'
              - 'devfile.yaml'

  build-tools-ansibleforge:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: >-
      always() && !failure() && (
        github.event_name == 'workflow_dispatch' && (inputs.image == 'tools-ansibleforge' || inputs.image == 'all') ||
        github.event_name == 'push' && needs.detect-changes.outputs.tools-ansibleforge == 'true'
      )
    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Red Hat registries
        run: |
          mkdir -p ~/.docker
          echo '${{ secrets.OCP_PULL_SECRET }}' > ~/.docker/config.json

      - name: Generate ansible.cfg for Red Hat Automation Hub
        env:
          AUTOMATIONHUB_TOKEN: ${{ secrets.AUTOMATIONHUB_TOKEN }}
        shell: bash
        run: |
          {
            echo "[galaxy]"
            echo "server_list = certified, validated, galaxy"
            echo ""
            echo "[galaxy_server.certified]"
            echo "url=https://console.redhat.com/api/automation-hub/content/published/"
            echo "auth_url=https://sso.redhat.com/auth/realms/redhat-external/protocol/openid-connect/token"
            echo "token=${AUTOMATIONHUB_TOKEN}"
            echo ""
            echo "[galaxy_server.validated]"
            echo "url=https://console.redhat.com/api/automation-hub/content/validated/"
            echo "auth_url=https://sso.redhat.com/auth/realms/redhat-external/protocol/openid-connect/token"
            echo "token=${AUTOMATIONHUB_TOKEN}"
            echo ""
            echo "[galaxy_server.galaxy]"
            echo "url=https://galaxy.ansible.com/"
          } > containers/tools-ansibleforge/ansible.cfg

      - name: Log in to ghcr.io
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push tools-ansibleforge
        uses: docker/build-push-action@v7
        with:
          context: containers/tools-ansibleforge
          file: containers/tools-ansibleforge/Containerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/tools-ansibleforge:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/tools-ansibleforge:${{ github.sha }}

  build-ee-ansibleforge:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: >-
      always() && !failure() && (
        github.event_name == 'workflow_dispatch' && (inputs.image == 'ee-ansibleforge' || inputs.image == 'all') ||
        github.event_name == 'push' && needs.detect-changes.outputs.ee-ansibleforge == 'true'
      )
    steps:
      - uses: actions/checkout@v6

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Red Hat registries
        run: |
          mkdir -p ~/.docker
          echo '${{ secrets.OCP_PULL_SECRET }}' > ~/.docker/config.json

      - name: Generate ansible.cfg for Red Hat Automation Hub
        env:
          AUTOMATIONHUB_TOKEN: ${{ secrets.AUTOMATIONHUB_TOKEN }}
        shell: bash
        run: |
          {
            echo "[galaxy]"
            echo "server_list = certified, validated, galaxy"
            echo ""
            echo "[galaxy_server.certified]"
            echo "url=https://console.redhat.com/api/automation-hub/content/published/"
            echo "auth_url=https://sso.redhat.com/auth/realms/redhat-external/protocol/openid-connect/token"
            echo "token=${AUTOMATIONHUB_TOKEN}"
            echo ""
            echo "[galaxy_server.validated]"
            echo "url=https://console.redhat.com/api/automation-hub/content/validated/"
            echo "auth_url=https://sso.redhat.com/auth/realms/redhat-external/protocol/openid-connect/token"
            echo "token=${AUTOMATIONHUB_TOKEN}"
            echo ""
            echo "[galaxy_server.galaxy]"
            echo "url=https://galaxy.ansible.com/"
          } > containers/ee-ansibleforge/ansible.cfg

      - name: Install ansible-builder
        run: pip install ansible-builder

      - name: Generate build context
        working-directory: containers/ee-ansibleforge
        run: ansible-builder create --file execution-environment.yaml

      - name: Log in to ghcr.io
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push ee-ansibleforge
        uses: docker/build-push-action@v7
        with:
          context: containers/ee-ansibleforge/context
          file: containers/ee-ansibleforge/context/Containerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/ee-ansibleforge:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/ee-ansibleforge:${{ github.sha }}

  build-keycloak-github-org-mapper:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: >-
      always() && !failure() && (
        github.event_name == 'workflow_dispatch' && (inputs.image == 'keycloak-github-org-mapper' || inputs.image == 'all') ||
        github.event_name == 'push' && needs.detect-changes.outputs.keycloak-github-org-mapper == 'true'
      )
    steps:
      - uses: actions/checkout@v6

      - name: Set up JDK
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: "17"

      - name: Maven build
        working-directory: keycloak-extensions/github-org-mapper
        run: mvn -B clean package -DskipTests

      - name: Log in to ghcr.io
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push keycloak-github-org-mapper
        uses: docker/build-push-action@v7
        with:
          context: keycloak-extensions/github-org-mapper
          file: keycloak-extensions/github-org-mapper/Containerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/keycloak-github-org-mapper:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/keycloak-github-org-mapper:${{ github.sha }}

  build-devfile-registry:
    runs-on: ubuntu-latest
    needs: detect-changes
    if: >-
      always() && !failure() && (
        github.event_name == 'workflow_dispatch' && (inputs.image == 'devfile-registry' || inputs.image == 'all') ||
        github.event_name == 'push' && needs.detect-changes.outputs.devfile-registry == 'true'
      )
    steps:
      - uses: actions/checkout@v6

      - name: Log in to ghcr.io
        uses: docker/login-action@v4
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push devfile-registry
        uses: docker/build-push-action@v7
        with:
          context: containers/devfile-registry
          file: containers/devfile-registry/Containerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/devfile-registry:latest
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}/devfile-registry:${{ github.sha }}
