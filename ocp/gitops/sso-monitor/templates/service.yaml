apiVersion: v1
kind: Service
metadata:
  name: sso-monitor
  namespace: {{ .Values.namespace }}
  labels:
    app: sso-monitor
spec:
  selector:
    app: sso-monitor
  ports:
  - name: metrics
    port: 9090
    targetPort: metrics
    protocol: TCP
