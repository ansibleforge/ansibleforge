# Injected by the bootstrap app-of-apps (helm parameter), e.g. "apps.ocp.spark.ansibleforge.dev".
clusterDomain: ""

image:
  # ghcr namespace the container-build workflow pushes to (ghcr.io/<owner>).
  # NOTE: confirm the owner matches where CI publishes (github.repository_owner).
  registry: ghcr.io/ansibleforge
  portalTag: latest
  gatewayTag: latest
  # Name of the image pull secret for the (private) ghcr vm-portal images.
  # Rendered as a Vault-backed dockerconfigjson ExternalSecret of this name
  # (templates/ghcr-pull-secret.yaml) and attached to the pod. Requires Vault
  # entries vm-portal/ghcr-username + vm-portal/ghcr-token (a read:packages PAT).
  # Set to "" instead if you make the ghcr packages public — then no pull
  # secret / Vault entry is needed.
  pullSecret: "vm-portal-ghcr"

gateway:
  awsRegion: us-east-2
  defaultSshUser: ec2-user
  requireManagedByAnsible: true
  # Sandbox-tolerant default carried over from the app: skips verification of
  # the AAP TLS cert. TODO: set false once the in-cluster AAP route presents a
  # CA-trusted cert to the gateway.
  aapTlsInsecure: true
  idleTimeoutMs: 600000
  maxSessionMs: 3600000
  maxSessions: 50
  maxSessionsPerUser: 5
